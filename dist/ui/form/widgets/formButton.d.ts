import * as React from 'react';
import { Context } from '../context';
import { ButtonSchema } from '../schema';
export declare const FormButton: ({ context, itemSchema, children }: {
    context: Context;
    itemSchema: ButtonSchema;
    children: React.ReactNode;
}) => JSX.Element;
